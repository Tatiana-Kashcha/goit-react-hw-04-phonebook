import * as s from './Logout.styled';

export const Logout = () => {
  const handleLogout = () => {};

  return (
    <>
      <s.ButtonAuth onClick={handleLogout}>Logout</s.ButtonAuth>
    </>
  );
};
